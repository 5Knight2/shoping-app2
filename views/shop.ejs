<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/product.css">
</head>

<body>
    <header class="main-header">
        <ul class="main-header__item-list">
            <li class="main-header__item"><a href="/add-product">add-product</a></li>
            
            <li class="main-header__item"><a href="/shop">shop</a></li>
            <li class="main-header__item"><a href="/contact-us">contact us</a></li>
        </ul>
    </header>

    <main>
        <h1>My Products</h1>
        <p>List of all the products...</p>
        <% if (prods.length > 0) { %>
            <div class="grid">
                <% for (let product of prods) { %>
                    <article class="card product-item">
                        <header class="card__header">
                            <h1 class="product__title"><%=product.title %></h1>
                        </header>
                       
                        <div class="card__content">
                            <h2 class="product__price"><%=product.price %></h2>
                            <p class="product__description"><%=product.description %>
                            </p>
                        </div>
                        <div class="card__actions">
                            <form action="/shop" method="post">
                                <input type="hidden" value="<%=product.id%>" name="productid">
                                <input type="hidden" value="<%=product.price%>" name="price">
                            <button class="btn">Add to Cart</button>
                            </form>
                        </div>
                        <div class="card__actions">
                            <a href="/add-product/<%=product.id%>">

                            <button  class="btn" name="details" id="details">Details</button></a>
                        </div>
                        <div class="card__actions">
                            <a href="/add-product/<%=product.id%>?edit=true">

                            <button  class="btn" name="edit" id="edit">Edit</button></a>
                        </div>
                </article>
                <% } %>
            </div>
        <% } else { %>
            <h1>No Products Found!</h1>
        <% } %>
    </main>
</body>

</html>